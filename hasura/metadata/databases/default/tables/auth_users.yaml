table:
  name: users
  schema: auth
object_relationships:
- name: profile
  using:
    foreign_key_constraint_on:
      column: user_id
      table:
        name: profiles
        schema: auth
array_relationships:
- name: user_sessions
  using:
    foreign_key_constraint_on:
      column: user_id
      table:
        name: user_sessions
        schema: auth
